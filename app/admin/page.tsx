import {prisma} from "../../lib/prisma";
export default async function Admin(){const [orders,products,users]=await Promise.all([prisma.order.count(),prisma.product.count(),prisma.user.count()]);return <main style={{fontFamily:"Arial",padding:40,background:"#0a0a0a",color:"white",minHeight:"100vh"}}><h1>TEEFLEX ADMIN</h1><div style={{display:"flex",gap:20,flexWrap:"wrap"}}>{[["Orders",orders],["Products",products],["Customers",users]].map(([a,b])=><div key={String(a)} style={{background:"#181818",padding:30,minWidth:180,border:"1px solid #333"}}><small>{a}</small><h2>{b}</h2></div>)}</div><p style={{color:"#aaa",marginTop:30}}>Next: protect this route with admin authentication and add product/order management forms.</p></main>}